project.version('1.0.14-SNAPSHOT')

dependencies {
    compile "com.kon.framework:kon-core:1.0.1-SNAPSHOT"

    compile "org.apache.commons:commons-lang3"

    compile "org.mybatis.spring.boot:mybatis-spring-boot-starter"
}
